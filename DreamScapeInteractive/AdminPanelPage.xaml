<Page
    x:Class="DreamScapeInteractive.AdminPanelPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScapeInteractive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Light">

    <Grid ColumnSpacing="10" Background="#3b2f2f">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Medieval Header -->
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Background="#8b5a2b">
            <TextBlock Text="Admin Panel" 
                       FontSize="36" 
                       FontWeight="Bold"
                       FontFamily="Garamond"
                       Foreground="Gold"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Margin="0,15,0,0"/>
            <Button  Margin="10,0,0,0" CornerRadius="10,0,0,10" x:Name="BackButton"  Content="Logout" Click="BackButton_Click" Background="#704214" Foreground="White" BorderBrush="Gold"/>

        </Grid>

        <!-- Buttons -->
        <Button x:Name="CatalogusRouteButton" 
                Grid.Row="1" 
                MinHeight="220" MinWidth="220" 
                Background="#704214"
                BorderBrush="Gold"
                BorderThickness="3"
                FontSize="20"
                Foreground="White"
                FontFamily="Garamond"
                HorizontalAlignment="Center" 
                Grid.Column="1" 
                Content="📜 Item Catalogus"
                Click="CatalogusRouteButton_Click"/>

        <Button x:Name="RegisterRouteButton" 
                Grid.Row="1" 
                MinHeight="220" MinWidth="220" 
                Background="#704214"
                BorderBrush="Gold"
                BorderThickness="3"
                FontSize="20"
                Foreground="White"
                FontFamily="Garamond"
                HorizontalAlignment="Center" 
                Grid.Column="2" 
                Content="⚔ Register Users"
                Click="RegisterRouteButton_Click"/>

    </Grid>
</Page>
