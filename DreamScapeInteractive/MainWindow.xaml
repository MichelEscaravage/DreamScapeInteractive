<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DreamScapeInteractive.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScapeInteractive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock x:Name="ItemNameBlock"/>
                        <TextBlock x:Name="ItemDescriptionBlock" TextWrapping="WrapWholeWords"/>
                        <TextBlock x:Name="ItemTypeDescriptionBlock"/>
                        <TextBlock x:Name="ItemRarityBlock"/>
                        <TextBlock x:Name="ItemPowerBlock"/>
                        <TextBlock x:Name="ItemSpeedBlock"/>
                        <TextBlock x:Name="ItemDurabilityBlock"/>
                        <TextBlock x:Name="ItemMagicPropertyBlock"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Window>
