<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="DreamScapeInteractive.Dialogues.ChangePasswordDialogue"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DreamScapeInteractive.Dialogues"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#3E2723"
    Foreground="#FFD700">

    <Grid RowSpacing="10" Padding="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Logo Image -->
        <Image Grid.Row="0" Grid.ColumnSpan="2" Source="ms-appx:///Assets\Logo.webp" Height="80" Stretch="Uniform" HorizontalAlignment="Center"/>

        <!-- Label above password box -->
        <TextBlock x:Name="PasswordLabel" Grid.ColumnSpan="2" Grid.Row="1" Text="For your safety, please change your password." FontSize="16" FontFamily="Garamond" TextAlignment="Center"/>

        <!-- Password box for new password -->
        <PasswordBox Grid.ColumnSpan="2" Grid.Row="2" x:Name="NewPasswordBox" PlaceholderText="New Password.." Background="#5D4037" Foreground="White"/>

        <!-- Password box for confirm new password -->
        <PasswordBox Grid.ColumnSpan="2" Grid.Row="3" x:Name="ConfirmNewPasswordBox" PlaceholderText="Confirm Password.." Background="#5D4037" Foreground="White"/>

        <!-- Button to change password -->
        <Button Margin="0,10,0,0" Content="Change Password" Grid.Row="5" x:Name="ChangePasswordButton" Click="ChangePasswordButton_Click" Background="#795548" Foreground="White"/>

        <!-- Button to cancel change password process -->
        <Button Margin="0,10,0,0" Content="Cancel" HorizontalAlignment="Right" Grid.Row="5" Grid.Column="1" x:Name="CancelButton" Click="CancelButton_Click" Background="#795548" Foreground="White"/>

    </Grid>
</ContentDialog>
